// If life was easy, we could just do things the easy way:
// var getElementsByClassName = function (className) {
//   return document.getElementsByClassName(className);
// };

// But instead we're going to implement it from scratch:
var getElementsByClassName = function(className) {

  var checkClassname = function () {
    //

  };

  var checkChildren = function () {

  };

  // create results array
  var checkElement = function () {

    // Check the current element if it has the classname
    // if it matches, add it to matchingElements array
    // Check current element if it has child elements
    // for each child element, call recursive function

  };



  var matchingElements = [];

  // return results array
  return;
};